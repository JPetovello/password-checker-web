diff --git a/app.py b/app.py
index f35bc39..f17bc44 100644
--- a/app.py
+++ b/app.py
@@ -27,6 +27,7 @@ INSTALL_SOURCE = os.environ.get("INSTALL_SOURCE", "DockerHub / Manual")
 
 # Load full EFF Large Wordlist, preserving original casing
 EFF_WORDS = []
+USING_FALLBACK_WORDLIST = False
 WORDLIST_PATH = os.path.join(os.path.dirname(__file__), 'eff_large_wordlist.txt')
 
 if os.path.exists(WORDLIST_PATH):
@@ -39,8 +40,19 @@ if os.path.exists(WORDLIST_PATH):
                 EFF_WORDS.append(parts[0])
     print(f"[Wordlist] Loaded {len(EFF_WORDS)} words from {WORDLIST_PATH}")
 else:
-    print(f"[Wordlist Warning] {WORDLIST_PATH} not found. Using fallback list.")
-    EFF_WORDS = ["correct", "horse", "battery", "staple", "dragon", "subway", "security"]
+    USING_FALLBACK_WORDLIST = True
+    print(f"[Wordlist Warning] {WORDLIST_PATH} not found. Using expanded emergency fallback list.")
+    EFF_WORDS = [
+        "correct", "horse", "battery", "staple", "dragon", "subway", "security",
+        "anchor", "bison", "cobalt", "canyon", "dolphin", "echo", "falcon",
+        "glacier", "harbor", "island", "jungle", "kettle", "lantern", "magnet",
+        "neutron", "oasis", "pinnacle", "quartz", "radar", "sierra", "timber",
+        "uranium", "vortex", "walrus", "xenon", "yellow", "zephyr", "avalanche",
+        "blizzard", "compass", "domino", "eclipse", "fossil", "granite", "horizon",
+        "igloo", "javelin", "kingdom", "leopard", "monsoon", "nebula", "octopus",
+        "pyramid", "quantum", "redwood", "saturn", "tsunami", "umbrella", "volcano",
+        "whisper", "zodiac", "alpine", "beacon", "cascade", "dune", "emerald"
+    ]
 
 @app.after_request
 def apply_security_headers(response):
@@ -127,7 +139,7 @@ def calculate_entropy(password):
 
 @app.route('/', methods=['GET'])
 def index():
-    return render_template('index.html', app_version=APP_VERSION)
+    return render_template('index.html', app_version=APP_VERSION, is_fallback=USING_FALLBACK_WORDLIST)
 
 @app.route('/api/evaluate', methods=['POST'])
 @limiter.limit("15 per minute")
@@ -174,7 +186,8 @@ def generate_passphrase():
 
     return jsonify({
         'passphrase': passphrase,
-        'words': selected_words
+        'words': selected_words,
+        'is_fallback': USING_FALLBACK_WORDLIST
     })
 
 if __name__ == '__main__':
diff --git a/index.html b/index.html
new file mode 100644
index 0000000..e63aaa1
--- /dev/null
+++ b/index.html
@@ -0,0 +1,617 @@
+<!DOCTYPE html>
+<html lang="en">
+<head>
+    <meta charset="UTF-8">
+    <meta name="viewport" content="width=device-width, initial-scale=1.0">
+    <title>Password & Passphrase Analyzer</title>
+    
+    <!-- Google Fonts for modern typography -->
+    <link rel="preconnect" href="https://fonts.googleapis.com">
+    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
+    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=JetBrains+Mono:wght@500;700&display=swap" rel="stylesheet">
+    
+    <script>
+        (function() {
+            const savedTheme = localStorage.getItem('pcw-theme');
+            const systemDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
+            const theme = savedTheme || (systemDark ? 'dark' : 'light');
+            if (theme === 'dark') {
+                document.documentElement.classList.add('dark');
+            } else {
+                document.documentElement.classList.remove('dark');
+            }
+        })();
+    </script>
+
+    <style>
+        /* CSS Reset & Variables */
+        :root {
+            --bg-body: #f8fafc;
+            --bg-main: #ffffff;
+            --bg-card: #f1f5f9;
+            --bg-box: #ffffff;
+            --border-color: #e2e8f0;
+            --border-focus: #0284c7;
+            
+            --text-primary: #0f172a;
+            --text-heading: #0369a1;
+            --text-muted: #64748b;
+            
+            --input-bg: #ffffff;
+            --btn-primary: #0284c7;
+            --btn-primary-hover: #0369a1;
+            --btn-text: #ffffff;
+            
+            --meter-0: #ef4444;
+            --meter-1: #f97316;
+            --meter-2: #eab308;
+            --meter-3: #0284c7;
+            --meter-4: #10b981;
+            --meter-bg: #e2e8f0;
+
+            --status-safe: #16a34a;
+            --status-danger: #dc2626;
+
+            --card-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.05), 0 8px 10px -6px rgba(0, 0, 0, 0.01);
+            --glow-color: rgba(2, 132, 199, 0.15);
+        }
+
+        html.dark {
+            --bg-body: #090d16;
+            --bg-main: #0f172a;
+            --bg-card: #1e293b;
+            --bg-box: #0f172a;
+            --border-color: #334155;
+            --border-focus: #38bdf8;
+            
+            --text-primary: #f8fafc;
+            --text-heading: #38bdf8;
+            --text-muted: #94a3b8;
+            
+            --input-bg: #020617;
+            --btn-primary: #0284c7;
+            --btn-primary-hover: #38bdf8;
+            --btn-text: #ffffff;
+            
+            --meter-0: #f87171;
+            --meter-1: #fb923c;
+            --meter-2: #facc15;
+            --meter-3: #38bdf8;
+            --meter-4: #4ade80;
+            --meter-bg: #334155;
+
+            --status-safe: #4ade80;
+            --status-danger: #f87171;
+
+            --card-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.5), 0 8px 10px -6px rgba(0, 0, 0, 0.3);
+            --glow-color: rgba(56, 189, 248, 0.15);
+        }
+
+        * {
+            box-sizing: border-box;
+        }
+
+        body { 
+            font-family: 'Inter', system-ui, -apple-system, sans-serif; 
+            display: flex; 
+            flex-direction: column; 
+            justify-content: center; 
+            align-items: center; 
+            min-height: 100vh; 
+            margin: 0; 
+            background-color: var(--bg-body); 
+            color: var(--text-primary); 
+            transition: background-color 0.25s ease, color 0.25s ease;
+            padding: 1.5rem;
+        }
+
+        main { 
+            background: var(--bg-main); 
+            padding: 2.5rem; 
+            border-radius: 1rem; 
+            box-shadow: var(--card-shadow); 
+            width: 100%; 
+            max-width: 850px; 
+            border: 1px solid var(--border-color);
+            transition: background 0.25s ease, border-color 0.25s ease;
+        }
+
+        /* Header Layout */
+        .header-row {
+            display: flex;
+            justify-content: space-between;
+            align-items: center;
+            margin-bottom: 2rem;
+            padding-bottom: 1rem;
+            border-bottom: 1px solid var(--border-color);
+        }
+
+        .header-title {
+            display: flex;
+            align-items: center;
+            gap: 0.75rem;
+        }
+
+        .header-title svg {
+            width: 28px;
+            height: 28px;
+            fill: none;
+            stroke: var(--text-heading);
+            stroke-width: 2;
+            stroke-linecap: round;
+            stroke-linejoin: round;
+        }
+
+        .header-row h1 {
+            margin: 0;
+            font-size: 1.5rem;
+            font-weight: 800;
+            letter-spacing: -0.025em;
+            color: var(--text-primary);
+        }
+
+        .theme-toggle-btn {
+            background: var(--input-bg);
+            border: 1px solid var(--border-color);
+            color: var(--text-primary);
+            padding: 0.5rem 0.85rem;
+            border-radius: 0.5rem;
+            cursor: pointer;
+            display: inline-flex;
+            align-items: center;
+            gap: 0.5rem;
+            font-size: 0.875rem;
+            font-weight: 600;
+            transition: all 0.2s ease;
+        }
+
+        .theme-toggle-btn:hover {
+            background: var(--bg-card);
+            border-color: var(--border-focus);
+        }
+
+        /* Card Elements */
+        .card { 
+            background: var(--bg-card); 
+            border: 1px solid var(--border-color); 
+            padding: 1.75rem; 
+            border-radius: 0.75rem; 
+            margin-bottom: 1.5rem; 
+            transition: all 0.25s ease;
+        }
+
+        .card h2 { 
+            margin-top: 0; 
+            font-size: 1.15rem; 
+            font-weight: 700;
+            margin-bottom: 1.25rem; 
+            color: var(--text-heading); 
+            display: flex;
+            align-items: center;
+            gap: 0.5rem;
+        }
+
+        /* Password Input Wrapper */
+        .input-wrapper {
+            position: relative;
+            margin-top: 0.5rem;
+            margin-bottom: 1.25rem;
+        }
+
+        input[type="text"], input[type="password"], select { 
+            width: 100%; 
+            padding: 0.85rem 1rem; 
+            border-radius: 0.5rem; 
+            border: 1.5px solid var(--border-color); 
+            background: var(--input-bg); 
+            color: var(--text-primary); 
+            font-family: 'JetBrains Mono', monospace;
+            font-size: 1rem; 
+            font-weight: 500;
+            text-align: left; 
+            transition: all 0.2s ease;
+        }
+
+        .password-input {
+            padding-right: 3rem !important;
+        }
+
+        .toggle-visibility-btn {
+            position: absolute;
+            right: 0.75rem;
+            top: 50%;
+            transform: translateY(-50%);
+            background: transparent;
+            border: none;
+            color: var(--text-muted);
+            cursor: pointer;
+            padding: 0.25rem;
+            display: flex;
+            align-items: center;
+            justify-content: center;
+            border-radius: 0.25rem;
+            width: auto;
+            margin: 0;
+        }
+
+        .toggle-visibility-btn:hover {
+            color: var(--text-primary);
+            background: transparent;
+        }
+
+        input:focus, select:focus { 
+            outline: none;
+            border-color: var(--border-focus);
+            box-shadow: 0 0 0 4px var(--glow-color);
+        }
+
+        select {
+            font-family: 'Inter', sans-serif;
+            cursor: pointer;
+            appearance: none;
+            background-image: url("data:image/svg+xml;charset=UTF-8,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'%3e%3cpolyline points='6 9 12 15 18 9'%3e%3c/polyline%3e%3c/svg%3e");
+            background-repeat: no-repeat;
+            background-position: right 1rem center;
+            background-size: 1.1em;
+            padding-right: 2.5rem;
+        }
+
+        /* Buttons */
+        button { 
+            width: 100%;
+            padding: 0.85rem 1.25rem;
+            margin-top: 0.5rem;
+            margin-bottom: 0;
+            border-radius: 0.5rem;
+            background: var(--btn-primary); 
+            color: var(--btn-text); 
+            border: none; 
+            cursor: pointer; 
+            font-size: 0.95rem;
+            font-weight: 700; 
+            font-family: 'Inter', sans-serif;
+            transition: all 0.2s ease; 
+            display: inline-flex;
+            align-items: center;
+            justify-content: center;
+            gap: 0.5rem;
+        }
+
+        button:hover { 
+            background: var(--btn-primary-hover);
+            transform: translateY(-1px);
+        }
+
+        button:active {
+            transform: translateY(0);
+        }
+
+        /* Grid Metrics Structure */
+        .grid { 
+            display: grid; 
+            grid-template-columns: repeat(3, 1fr); 
+            gap: 1rem; 
+            margin-top: 1rem; 
+        }
+
+        @media (max-width: 640px) {
+            .grid {
+                grid-template-columns: 1fr;
+            }
+        }
+
+        .metric-box { 
+            background: var(--bg-box); 
+            padding: 1.1rem; 
+            border-radius: 0.5rem; 
+            text-align: center; 
+            border: 1px solid var(--border-color); 
+            display: flex;
+            flex-direction: column;
+            justify-content: center;
+            transition: all 0.2s ease;
+        }
+
+        .metric-title { 
+            font-size: 0.725rem; 
+            font-weight: 700;
+            color: var(--text-muted); 
+            text-transform: uppercase; 
+            letter-spacing: 0.05em; 
+            margin-bottom: 0.4rem; 
+        }
+
+        .metric-value { 
+            font-family: 'JetBrains Mono', monospace;
+            font-size: 1.15rem; 
+            font-weight: 700; 
+            color: var(--text-primary); 
+        }
+
+        /* Meter Bar Dynamic Display */
+        .meter-container {
+            width: 100%;
+            height: 6px;
+            background-color: var(--meter-bg);
+            border-radius: 9999px;
+            margin-top: 0.75rem;
+            overflow: hidden;
+            display: block;
+        }
+
+        .meter-bar {
+            height: 100%;
+            width: 0%;
+            border-radius: 9999px;
+            transition: width 0.4s cubic-bezier(0.4, 0, 0.2, 1), background-color 0.4s ease;
+        }
+
+        /* Generator Section Layout */
+        .generator-row { 
+            display: flex; 
+            gap: 0.75rem; 
+            align-items: center; 
+        }
+
+        .generator-row input { 
+            margin-bottom: 0; 
+            margin-top: 0;
+            flex-grow: 1;
+        }
+
+        .generator-row button { 
+            width: 130px; 
+            margin-top: 0; 
+            margin-bottom: 0; 
+            height: 48px; 
+            flex-shrink: 0;
+        }
+
+        .status-badge {
+            display: inline-block;
+            font-weight: 700;
+            transition: color 0.2s ease;
+        }
+    </style>
+</head>
+<body>
+    <main>
+        <div class="header-row">
+            <div class="header-title">
+                <svg viewBox="0 0 24 24">
+                    <rect x="3" y="11" width="18" height="11" rx="2" ry="2"></rect>
+                    <path d="M7 11V7a5 5 0 0 1 10 0v4"></path>
+                </svg>
+                <h1>Password & Passphrase Analyzer</h1>
+            </div>
+            
+            <button 
+                id="theme-toggle" 
+                class="theme-toggle-btn" 
+                type="button" 
+                aria-label="Switch theme"
+                aria-pressed="false"
+            >
+                <span id="theme-icon" aria-hidden="true">🌙</span>
+                <span id="theme-text">Dark</span>
+            </button>
+        </div>
+        
+        <!-- Password Evaluator Section -->
+        <div class="card">
+            <h2>Password Strength Evaluator</h2>
+            <div class="input-wrapper">
+                <input type="password" id="passwordInput" class="password-input" placeholder="Enter password to analyze..." aria-label="Password to evaluate">
+                <button type="button" id="togglePasswordBtn" class="toggle-visibility-btn" aria-label="Toggle password visibility">
+                    <svg id="eyeIcon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
+                        <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"></path>
+                        <circle cx="12" cy="12" r="3"></circle>
+                    </svg>
+                </button>
+            </div>
+            
+            <div class="grid">
+                <div class="metric-box">
+                    <div class="metric-title">Score</div>
+                    <div class="metric-value" id="scoreVal">- / 4</div>
+                    <div class="meter-container" aria-hidden="true">
+                        <div id="meterBar" class="meter-bar"></div>
+                    </div>
+                </div>
+                <div class="metric-box">
+                    <div class="metric-title">Entropy</div>
+                    <div class="metric-value" id="entropyVal">0 bits</div>
+                </div>
+                <div class="metric-box">
+                    <div class="metric-title">Length</div>
+                    <div class="metric-value" id="lengthVal">0 chars</div>
+                </div>
+            </div>
+
+            <div class="grid">
+                <div class="metric-box">
+                    <div class="metric-title">Online Attack</div>
+                    <div class="metric-value" id="onlineVal" style="font-size: 0.95rem;">-</div>
+                </div>
+                <div class="metric-box">
+                    <div class="metric-title">Offline Fast Hash (GPU)</div>
+                    <div class="metric-value" id="fastHashVal" style="font-size: 0.95rem;">-</div>
+                </div>
+                <div class="metric-box">
+                    <div class="metric-title">Offline Slow Hash</div>
+                    <div class="metric-value" id="slowHashVal" style="font-size: 0.95rem;">-</div>
+                </div>
+            </div>
+
+            <div class="metric-box" style="margin-top: 1rem;">
+                <div class="metric-title">Breach Exposure (HIBP)</div>
+                <div class="metric-value" id="hibpVal" style="font-size: 0.95rem; font-weight: 600;">-</div>
+            </div>
+        </div>
+
+        <!-- Passphrase Generator Section -->
+        <div class="card" style="margin-bottom: 0;">
+            <h2>EFF Passphrase Generator</h2>
+            <div class="generator-row">
+                <input type="text" id="passphraseOutput" readonly placeholder="Click generate below...">
+                <button type="button" id="copyBtn">Copy</button>
+            </div>
+            
+            <label for="wordCountSelect" style="display: block; margin-top: 1.25rem; font-size: 0.85rem; font-weight: 700; color: var(--text-muted); text-transform: uppercase; letter-spacing: 0.05em;">Number of Words</label>
+            <select id="wordCountSelect" style="margin-top: 0.4rem; margin-bottom: 1rem;">
+                <option value="3">3 Words</option>
+                <option value="4" selected>4 Words</option>
+                <option value="5">5 Words</option>
+                <option value="6">6 Words</option>
+                <option value="7">7 Words</option>
+                <option value="8">8 Words</option>
+            </select>
+
+            <button type="button" id="generateBtn">Generate Passphrase</button>
+        </div>
+    </main>
+
+    {% include 'footer.html' %}
+
+    <script>
+        // Theme Toggle Logic
+        const toggleBtn = document.getElementById('theme-toggle');
+        const themeIcon = document.getElementById('theme-icon');
+        const themeText = document.getElementById('theme-text');
+
+        function updateThemeUI(isDark) {
+            toggleBtn.setAttribute('aria-pressed', isDark);
+            toggleBtn.setAttribute('aria-label', `Switch to ${isDark ? 'light' : 'dark'} theme`);
+            themeIcon.textContent = isDark ? '☀️' : '🌙';
+            themeText.textContent = isDark ? 'Light' : 'Dark';
+        }
+
+        const isDarkInitial = document.documentElement.classList.contains('dark');
+        updateThemeUI(isDarkInitial);
+
+        toggleBtn.addEventListener('click', () => {
+            const isDarkNow = document.documentElement.classList.toggle('dark');
+            localStorage.setItem('pcw-theme', isDarkNow ? 'dark' : 'light');
+            updateThemeUI(isDarkNow);
+        });
+
+        // Password Input Visibility Toggle
+        const passwordInput = document.getElementById('passwordInput');
+        const togglePasswordBtn = document.getElementById('togglePasswordBtn');
+        const eyeIcon = document.getElementById('eyeIcon');
+
+        togglePasswordBtn.addEventListener('click', () => {
+            const isPassword = passwordInput.type === 'password';
+            passwordInput.type = isPassword ? 'text' : 'password';
+            
+            eyeIcon.innerHTML = isPassword 
+                ? `<path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19m-6.72-1.07a3 3 0 1 1-4.24-4.24"></path><line x1="1" y1="1" x2="23" y2="23"></line>`
+                : `<path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"></path><circle cx="12" cy="12" r="3"></circle>`;
+        });
+
+        // Passphrase Generator Action
+        document.getElementById('generateBtn').addEventListener('click', async () => {
+            const wordCount = document.getElementById('wordCountSelect').value;
+            try {
+                const response = await fetch(`/api/generate?words=${wordCount}`);
+                const data = await response.json();
+                if (data.passphrase) {
+                    document.getElementById('passphraseOutput').value = data.passphrase;
+                }
+            } catch (err) {
+                console.error('Failed to generate passphrase', err);
+            }
+        });
+
+        // Clipboard Copy Action
+        document.getElementById('copyBtn').addEventListener('click', () => {
+            const output = document.getElementById('passphraseOutput');
+            if (!output.value) return;
+
+            output.select();
+            output.setSelectionRange(0, 99999);
+
+            try {
+                const successful = document.execCommand('copy');
+                if (successful) {
+                    const copyBtn = document.getElementById('copyBtn');
+                    const originalText = copyBtn.innerText;
+                    copyBtn.innerText = 'Copied!';
+                    setTimeout(() => {
+                        copyBtn.innerText = originalText;
+                    }, 1500);
+                }
+            } catch (err) {
+                console.error('Fallback copy failed', err);
+            }
+        });
+
+        // Password Evaluator Trigger
+        passwordInput.addEventListener('input', (e) => {
+            evaluatePassword(e.target.value);
+        });
+
+        const meterColors = [
+            'var(--meter-0)',
+            'var(--meter-1)',
+            'var(--meter-2)',
+            'var(--meter-3)',
+            'var(--meter-4)'
+        ];
+
+        const meterWidths = ['15%', '35%', '55%', '80%', '100%'];
+
+        async function evaluatePassword(password) {
+            const meterBar = document.getElementById('meterBar');
+            document.getElementById('lengthVal').innerText = password.length + ' chars';
+            
+            if (!password) {
+                document.getElementById('scoreVal').innerText = '- / 4';
+                document.getElementById('scoreVal').style.color = 'var(--text-primary)';
+                document.getElementById('entropyVal').innerText = '0 bits';
+                document.getElementById('onlineVal').innerText = '-';
+                document.getElementById('fastHashVal').innerText = '-';
+                document.getElementById('slowHashVal').innerText = '-';
+                document.getElementById('hibpVal').innerText = '-';
+                document.getElementById('hibpVal').style.color = 'var(--text-primary)';
+                meterBar.style.width = '0%';
+                return;
+            }
+
+            try {
+                const response = await fetch('/api/evaluate', {
+                    method: 'POST',
+                    headers: { 'Content-Type': 'application/json' },
+                    body: JSON.stringify({ password: password })
+                });
+                const data = await response.json();
+                const score = (data.score !== undefined && data.score >= 0 && data.score <= 4) ? data.score : 0;
+                
+                document.getElementById('scoreVal').innerText = score + ' / 4';
+                document.getElementById('scoreVal').style.color = meterColors[score];
+
+                meterBar.style.width = meterWidths[score];
+                meterBar.style.backgroundColor = meterColors[score];
+
+                document.getElementById('entropyVal').innerText = (data.entropy !== undefined ? Math.round(data.entropy) : 0) + ' bits';
+                document.getElementById('onlineVal').innerText = data.crack_times_display?.online_throttling_100_per_hour || '-';
+                document.getElementById('fastHashVal').innerText = data.crack_times_display?.offline_fast_hashing_1e10_per_second || '-';
+                document.getElementById('slowHashVal').innerText = data.crack_times_display?.offline_slow_hashing_1e4_per_second || '-';
+                
+                if (data.hibp) {
+                    const hibpElement = document.getElementById('hibpVal');
+                    if (data.hibp.found) {
+                        hibpElement.innerText = `Found in ${data.hibp.count.toLocaleString()} breaches!`;
+                        hibpElement.style.color = 'var(--status-danger)';
+                    } else {
+                        hibpElement.innerText = 'No breaches found';
+                        hibpElement.style.color = 'var(--status-safe)';
+                    }
+                }
+            } catch (err) {
+                console.error('Evaluation error', err);
+            }
+        }
+    </script>
+</body>
+</html>
